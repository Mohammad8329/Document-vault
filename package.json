{
  "name": "document-vault",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "bun --hot src/server.ts",
    "gendb": "bunx prisma migrate dev",
    "test": "bun test",
    "test:integration": "bun test tests/integration",
    "typecheck": "tsc --noEmit",
    "sanity": "bun run typecheck && bun run test"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/node": "^26.3.0",
    "prisma": "5"
  },
  "peerDependencies": {
    "typescript": "^7.0.2"
  },
  "dependencies": {
    "@prisma/client": "5",
    "graphql": "^17.0.2",
    "graphql-yoga": "^5.22.0"
  }
}
